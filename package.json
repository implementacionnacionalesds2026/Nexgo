{
  "name": "nexgo-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for Railway deployment",
  "scripts": {
    "build": "cd nexgo-backend && npm install",
    "start": "cd nexgo-backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
